[package]
name = "echo"
version = "0.0.0"
description = "echo — a local opencode client"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
